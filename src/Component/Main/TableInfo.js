import React from "react";
import styled from "styled-components";

const TableInfo = () => {
  return <TableInfoContainer></TableInfoContainer>;
};

export default TableInfo;

const TableInfoContainer = styled.div`
  width: 40%;
  height: 800px;
  border: 1px solid blue;
`;
